name: deteksi_penyakit
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  image_picker: ^1.0.7
  shared_preferences: ^2.2.2
  fl_chart: ^0.66.0
  camera: ^0.11.1
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/model/yolov8n.tflite
    - assets/img/logo cengkeh.jpeg
    - assets/model/CLASSES.txt
